<section class="section image-with-text-section">
  <div class="page-width">
    <div class="image-with-text{% if section.settings.layout == 'image_second' %} image-with-text--reverse{% endif %}">

      <div class="image-with-text__media">
        {%- if section.settings.image != blank -%}
          {{
            section.settings.image
            | image_url: width: 900
            | image_tag:
              loading: 'lazy',
              width: 900,
              alt: section.settings.title
          }}
        {%- else -%}
          {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
        {%- endif -%}
      </div>

      <div class="image-with-text__content">
        {%- if section.settings.eyebrow != blank -%}
          <span class="image-with-text__eyebrow">{{ section.settings.eyebrow }}</span>
        {%- endif -%}

        {%- if section.settings.title != blank -%}
          <h2 class="image-with-text__title">{{ section.settings.title }}</h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <div class="image-with-text__text">{{ section.settings.text }}</div>
        {%- endif -%}

        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link }}" class="btn btn--primary">
            {{ section.settings.button_label }}
          </a>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Imagen con Texto",
  "tag": "section",
  "settings": [
    {
      "type": "select",
      "id": "layout",
      "label": "Diseño",
      "options": [
        { "value": "image_first", "label": "Imagen a la izquierda" },
        { "value": "image_second", "label": "Imagen a la derecha" }
      ],
      "default": "image_first"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Imagen"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Supertítulo"
    },
    {
      "type": "text",
      "id": "title",
      "default": "Sobre Nuestra Marca",
      "label": "Título"
    },
    {
      "type": "richtext",
      "id": "text",
      "default": "<p>Creamos piezas de la más alta calidad, pensadas para mujeres que valoran la elegancia y la exclusividad en el día a día.</p>",
      "label": "Texto"
    },
    {
      "type": "text",
      "id": "button_label",
      "default": "Saber más",
      "label": "Texto del botón"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Enlace del botón"
    }
  ],
  "presets": [
    {
      "name": "Imagen con Texto"
    }
  ]
}
{% endschema %}
