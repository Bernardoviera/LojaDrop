<section class="section image-with-text-section" style="background-color: {{ section.settings.bg_color }}; color: {{ section.settings.text_color_val }};">
  <div class="page-width">

    {%- if section.settings.show_logo -%}
      <div class="iwt-logo">
        {%- if section.settings.logo != blank -%}
          {{
            section.settings.logo
            | image_url: width: 240
            | image_tag: width: 160, alt: shop.name, class: 'iwt-logo__img'
          }}
        {%- else -%}
          <span class="iwt-logo__text">{{ shop.name }}</span>
        {%- endif -%}
      </div>
    {%- endif -%}

    {%- if section.settings.image != blank -%}
      <div class="image-with-text{% if section.settings.layout == 'image_second' %} image-with-text--reverse{% endif %}">
        <div class="image-with-text__media">
          {{
            section.settings.image
            | image_url: width: 900
            | image_tag: loading: 'lazy', width: 900, alt: section.settings.title
          }}
        </div>
        <div class="image-with-text__content">
          {%- if section.settings.eyebrow != blank -%}
            <span class="image-with-text__eyebrow">{{ section.settings.eyebrow }}</span>
          {%- endif -%}
          {%- if section.settings.title != blank -%}
            <h2 class="image-with-text__title">{{ section.settings.title }}</h2>
          {%- endif -%}
          {%- if section.settings.text != blank -%}
            <div class="image-with-text__text">{{ section.settings.text }}</div>
          {%- endif -%}
          {%- if section.settings.button_label != blank -%}
            <a href="{{ section.settings.button_link }}" class="btn btn--primary">{{ section.settings.button_label }}</a>
          {%- endif -%}
        </div>
      </div>
    {%- else -%}
      <div class="image-with-text__content image-with-text__content--centered">
        {%- if section.settings.eyebrow != blank -%}
          <span class="image-with-text__eyebrow">{{ section.settings.eyebrow }}</span>
        {%- endif -%}
        {%- if section.settings.title != blank -%}
          <h2 class="image-with-text__title">{{ section.settings.title }}</h2>
        {%- endif -%}
        {%- if section.settings.text != blank -%}
          <div class="image-with-text__text">{{ section.settings.text }}</div>
        {%- endif -%}
        {%- if section.settings.button_label != blank -%}
          <a href="{{ section.settings.button_link }}" class="btn btn--primary">{{ section.settings.button_label }}</a>
        {%- endif -%}
      </div>
    {%- endif -%}

  </div>
</section>

{% schema %}
{
  "name": "Imagem com Texto",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Fundo e cor"
    },
    {
      "type": "select",
      "id": "bg_color",
      "label": "Cor de fundo",
      "options": [
        { "value": "#ffffff", "label": "Branco" },
        { "value": "#f8f5f2", "label": "Creme (suave)" },
        { "value": "#1a1a1a", "label": "Preto elegante" },
        { "value": "#1a1a2e", "label": "Azul escuro" },
        { "value": "#c9a96e", "label": "Dourado" }
      ],
      "default": "#1a1a1a"
    },
    {
      "type": "select",
      "id": "text_color_val",
      "label": "Cor do texto",
      "options": [
        { "value": "#ffffff", "label": "Branco" },
        { "value": "#1a1a1a", "label": "Preto" }
      ],
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Logo"
    },
    {
      "type": "checkbox",
      "id": "show_logo",
      "label": "Mostrar logo",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo (opcional, se não enviado usa o nome da loja)"
    },
    {
      "type": "header",
      "content": "Conteúdo"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Layout (com imagem)",
      "options": [
        { "value": "image_first", "label": "Imagem à esquerda" },
        { "value": "image_second", "label": "Imagem à direita" }
      ],
      "default": "image_first"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Imagem (opcional)"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Supertítulo"
    },
    {
      "type": "text",
      "id": "title",
      "default": "Feita para fazer a mulher moderna brilhar",
      "label": "Título"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Texto"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Texto do botão"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Link do botão"
    }
  ],
  "presets": [
    {
      "name": "Imagem com Texto"
    }
  ]
}
{% endschema %}
