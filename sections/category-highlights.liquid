<section class="section">
  <div class="page-width">
    {%- if section.settings.title != blank -%}
      <div class="section-header">
        <h2 class="section-header__title">{{ section.settings.title }}</h2>
      </div>
    {%- endif -%}

    <div class="category-highlights">
      {%- for block in section.blocks -%}
        <a class="category-card" href="{{ block.settings.link }}" {{ block.shopify_attributes }}>
          <div class="category-card__media">
            {%- if block.settings.image != blank -%}
              {{
                block.settings.image
                | image_url: width: 800
                | image_tag: loading: 'lazy', width: 800, alt: block.settings.title
              }}
            {%- else -%}
              {{ 'collection-' | append: forloop.index | placeholder_svg_tag: 'placeholder-svg' }}
            {%- endif -%}
          </div>
          <div class="category-card__overlay"></div>
          <div class="category-card__content">
            <h3 class="category-card__title">{{ block.settings.title }}</h3>
            {%- if block.settings.cta_label != blank -%}
              <span class="category-card__link">{{ block.settings.cta_label }}</span>
            {%- endif -%}
          </div>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Categorías Destacadas",
  "tag": "section",
  "max_blocks": 4,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "Explora las Categorías",
      "label": "Título de la sección"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Categoría",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Imagen"
        },
        {
          "type": "text",
          "id": "title",
          "default": "Vestidos",
          "label": "Título"
        },
        {
          "type": "text",
          "id": "cta_label",
          "default": "Ver colección",
          "label": "Texto del enlace"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Enlace"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Categorías Destacadas",
      "blocks": [
        { "type": "category", "settings": { "title": "Vestidos" } },
        { "type": "category", "settings": { "title": "Joyas" } },
        { "type": "category", "settings": { "title": "Accesorios" } }
      ]
    }
  ]
}
{% endschema %}
