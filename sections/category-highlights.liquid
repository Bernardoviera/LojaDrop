<section class="section">
  <div class="page-width">
    {%- if section.settings.title != blank -%}
      <div class="section-header">
        <h2 class="section-header__title">{{ section.settings.title }}</h2>
      </div>
    {%- endif -%}

    <div class="category-highlights">
      {%- for block in section.blocks -%}
        <a class="category-card" href="{{ block.settings.link }}" {{ block.shopify_attributes }}>
          <div class="category-card__media">
            {%- if block.settings.image != blank -%}
              {{
                block.settings.image
                | image_url: width: 800
                | image_tag: loading: 'lazy', width: 800, alt: block.settings.title
              }}
            {%- else -%}
              {{ 'collection-' | append: forloop.index | placeholder_svg_tag: 'placeholder-svg' }}
            {%- endif -%}
          </div>
          <div class="category-card__overlay"></div>
          <div class="category-card__content">
            <h3 class="category-card__title">{{ block.settings.title }}</h3>
            {%- if block.settings.cta_label != blank -%}
              <span class="category-card__link">{{ block.settings.cta_label }}</span>
            {%- endif -%}
          </div>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Categorias em Destaque",
  "tag": "section",
  "max_blocks": 4,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "default": "Explore Nossas Categorias",
      "label": "Título da seção"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Categoria",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Imagem"
        },
        {
          "type": "text",
          "id": "title",
          "default": "Vestidos",
          "label": "Título"
        },
        {
          "type": "text",
          "id": "cta_label",
          "default": "Ver coleção",
          "label": "Texto do link"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Categorias em Destaque",
      "blocks": [
        { "type": "category", "settings": { "title": "Vestidos", "cta_label": "Ver coleção" } },
        { "type": "category", "settings": { "title": "Joias", "cta_label": "Ver coleção" } },
        { "type": "category", "settings": { "title": "Acessórios", "cta_label": "Ver coleção" } }
      ]
    }
  ]
}
{% endschema %}
